/**
 * TODO: one card per driver — planned value, actual value, a small
 * trend sparkline (Recharts is already a dependency), and a visual
 * indicator when this driver has an active DriftFlag. Don't just turn a
 * number red — surface what the drift means (see DriftFlag in
 * @signal-drift/types and the finance package's detectDrift).
 */
export default function DriverCard() {
  return null; // TODO
}
